{
	"vercel": {
		"command": "npx",
		"args": [
			"--yes",
			"mcp-remote",
			"https://mcp.vercel.com",
			"--header",
			"Authorization:${AUTH_HEADER}"
		],
		"env": {
			"AUTH_HEADER": "Bearer ${VERCEL_ACCESS_TOKEN}"
		}
	}
}
